<main class="page">
  <section class="cart dark">
    <div class="container">
      <div class="block-heading">
        <h2>User profile</h2>
      </div>
      <div class="content">
        <div class="row">
          <div class="col-md-12 col-lg-12">
            <h2 class="text-center pt-4">My Orders</h2>
            {{# each orders}}
            <div class="card text-center m-4">
              <div class="card-body">
                <ul class="list-group">
                  {{# each this.items}}
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    {{ this.item.title }} <em>{{ this.item.author }}</em> {{ this.qty }} Units
                    <span class="badge badge-primary badge-pill">{{ this.price }}</span>
                  </li>
                  {{/each}}
                </ul>
              </div>
              <div class="card-footer">
                <strong>Total Price: &euro; {{ this.cart.totalPrice }}</strong>
              </div>
            </div>
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  </section>
</main>
